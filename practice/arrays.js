const strings = ['a', 'b', 'c', 'd'];
/*
    on a 32 bit system each character will take 4 bytes to store.
    4*4 = 16
    this array will occupy 16 consecutive address blocks in computer's ram when being used
*/